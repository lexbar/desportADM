<li>
    <!-- timeline icon -->
    <i class="fa fa-pencil-square-o bg-aqua"></i>
    <div class="timeline-item">
        <span class="time"><i class="fa fa-clock-o"></i> {{ event.date | date('g:i a') }}</span>

        <h3 class="timeline-header"><a href="{{ path('desport_sales_client_view', {client_id: event.client.id}) }}">Anotación de cliente</a> {{ event.client.name }}</h3>

        <div class="timeline-body">
            Introducido por <strong>{{ event.user.name }}</strong>
            <p>{{ event.text }}</p>
        </div>

        <div class='timeline-footer'>
            <a href="{{ path('desport_sales_client_view', {client_id: event.client.id}) }}" class="btn btn-info btn-xs">Ver cliente</a>
        </div>
    </div>
</li>